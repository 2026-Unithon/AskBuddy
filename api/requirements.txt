# 공용 파일 — 추가할 땐 슬랙 공지 후 한 줄씩 append. 재정렬·재작성 금지
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0
asyncpg>=0.29
httpx>=0.27
python-multipart>=0.0.9

# 인증 (관호)
PyJWT>=2.9
passlib[bcrypt]>=1.7.4
bcrypt>=4.0
email-validator>=2.0

# 임베딩 — 관호님 코드가 이걸 쓴다. 새 임베딩 함수를 만들지 않는다 (D4)
openai>=1.40

# ingest (준혁)
google-genai>=0.3        # 구 google-generativeai 아님
pypdf>=4.3
pillow>=10.4
tenacity>=9.0
