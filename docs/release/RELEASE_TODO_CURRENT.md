# 현재 기능 릴리스 TODO

2026-09-16 · 목표: 현재 기능의 제한된 실사용 파일럿. 현재 출시 판정: **보류**.

`[x]` 명시한 범위 완료 / `[~]` 부분 완료 / `[ ]` 미완료.
계획 작성·코드 존재·unit 통과를 실제 사용성 개선이나 출시 통과로 체크하지 않는다.
범위/순서는 [계획](plan/USABILITY_RELEASE_PLAN.md), 측정은 [기준](plan/BASELINE_AND_ACCEPTANCE.md).

## REL-G. 페이지별 검토 전 전역 기반 — 먼저

- [x] 라우트·인증 hydration·bootstrap·Query 기본값·전역 navigation/조회·API 오류·공통 UI를 코드 검토했다.
- [x] 현재 Web 정적 검사와 production build를 통과했다. 이는 실제 사용성/런타임 API 인수가 아니다.
- [x] 서버 `/app/bootstrap`을 Web 진입 정본으로 연결하고 실제 존재하는 목적지와 맞춘다. 로그인/새로고침/직접 링크의 목적지를 보존한다.
- [x] production에서 mock/로컬 기본값을 실제 서버 빈 상태·오류 대신 보여주는 경로를 제거하고 데모 자격 증명은 `NEXT_PUBLIC_DEMO_MODE=true`에서만 노출한다.
- [x] 401/403/404/409/422/429/503/504·timeout·사용자 취소를 공통 API 오류로 분류하고 인증 만료 시 안전한 재로그인/목적지 복원을 제공한다.
- [x] Query별 freshness와 재조회 정책을 정하고, 하단 navigation의 대기 수 조회를 bootstrap/요약 응답으로 줄인다. mutation은 정확한 데이터만 갱신한다.
- [x] 공통 버튼/링크/입력에 최소 터치 크기, focus, pressed/loading/disabled, 중복 클릭 방지, inline 오류·재시도 계약을 적용한다.
- [x] 전역 타이포/간격/표면/상태 색/token을 정리하고 본문 16px·보조문구 가독성·safe area·reduced motion을 검증한다.
- [ ] 업로드/추출 외부 작업의 DB connection 수명, 제한 동시성, 단계별 상태/시간, 재시작 회수의 구현 경계를 확정한다.
- [ ] request/job/source/card ID를 잇는 운영 진단 로그와 실제 사용자 흐름 smoke를 만든다. 민감 원문은 일반 로그에 복제하지 않는다.

상세 순서는 [전역 기반 계획](plan/GLOBAL_FOUNDATION_PLAN.md)을 따른다. 이 작업은 docs/dev의 모델·채점·fact/card 의미 설계를 변경하지 않는다.

## REL-00. 재현과 현재 기준선

- [x] 릴리스 문서 트랙·책임 경계·초기 코드 관찰을 작성했다.
- [ ] 로컬 실행과 대상 배포의 코드/설정 차이, 실제 ingest mode/model, DB/Storage/권한을 확인한다. 비밀값은 기록하지 않는다.
- [ ] 익명/합성 PDF·이미지·카톡·음성·영상 작은 자료를 준비하고 고정 사실/기대 카드를 작성한다. holdout은 열지 않는다.
- [ ] 실제 화면에서 업로드→처리→카드→수정→승인을 재현한다. 실패/느림/혼란의 스크린샷·재현 절차를 기록한다.
- [ ] 버튼별 대기, API/DB 대기, 파일별 전송/전처리/STT/추출/조립/저장 시간을 측정한다.
- [ ] 우선 지원할 입력 유형·파일 크기/길이·파일럿 범위와 성능/품질 기준을 결과 확인 전에 고정한다.

## REL-01. 업로드·처리가 다른 사용을 막지 않게 — W, P0

- [ ] `process_source`의 장시간 DB connection 범위를 짧은 조회/저장으로 분리한다. 외부 호출 중 연결/lock을 점유하지 않는다.
- [ ] 파일 전송/해시/등록, source 처리의 제한된 동시성·timeout·취소/실패 예산을 실측 후 도입한다. 무제한 병렬화하지 않는다.
- [ ] 중복 업로드·작업 접수 실패 복구·재시도에서 성공 자료를 잃거나 중복 처리하지 않게 한다.
- [ ] 실제 접수/작업 ID를 보존하고 파일별 단계/부분 실패/재시도를 보여준다. 임의 퍼센트/남은 시간은 쓰지 않는다.
  - [ ] **점주 화면이 `PARTIAL` 을 표시한다.** 2026-09-19 서버가 구간 부분 실패를 정직하게 보고하기 시작했으나 `web/` 은 아직 이 상태를 모른다. `ingest_job_sources.status='PARTIAL'`·`error_code='PARTIAL_EXTRACTION'`·`segments_total`/`segments_failed` 를 화면 문구로 옮긴다. 성공과 부분 성공을 같은 모양으로 그리지 않는다. 작업 전 `web-async-state-check` 스킬을 쓴다.
  - [ ] **실패 구간 재처리 경로를 만든다.** `failed_segment_ids` 에 잃은 구간 이름이 남지만 "다시 시도" API·UI 가 없다. 자료 전체 재처리와 해당 구간만 재처리를 구분하고, 재처리가 이미 적힌 사실을 중복으로 만들지 않는지 확인한다(원장 중복 열쇠는 `unit`·`polarity`·`conditions`·`exceptions` 를 포함하도록 고쳤다).

  근거: [W/R 감사 2026-09-19](../dev/review/WR_IMPLEMENTATION_AUDIT_20260919.md) 결함 2, migration `20260919120000_w_partial_segment_extraction.sql`, 검증 `api/scripts/verify_w_partial_extraction.py`.
- [ ] 전송 중 화면 이탈의 제약과 서버 접수 후 백그라운드 처리를 구분한다. 화면 이동 가능과 worker 재시작 안전성을 혼동하지 않는다.
- [ ] 처리 중 카드 목록/수정/조회가 응답하는지, 프로세스 재시작 뒤 고아 작업을 회수하는지 검증한다. 회수 구현은 J2와 연결한다.

## REL-02. 지원 자료에서는 쓸 만한 카드가 나오게 — W, P0

- [ ] 유형별 전처리·추출·원장·조립 실패를 분리하고 실제 누락/오조립 원인을 확인한다.
- [ ] PDF 페이지/표/혼합 스캔, 이미지, 카톡 정정, 음성/STT, 영상 근거의 확인된 결함부터 작은 변경으로 수정한다.
- [ ] 출력 잘림/모델 오류/조립 누락을 성공으로 위장하지 않고 성공 사실 보존·검수 대기·재시도 경로를 제공한다.
- [ ] 같은 메뉴 HOT/ICE 한 카드의 규격별 값, 얼음 레시피 ICE, 수량/단위/부정/조건/순서를 검증한다.
- [ ] 실패 자료의 원본은 남기고 직접 카드 작성/수정 또는 다른 자료 추가의 실제 가능한 대안을 제공한다. 없는 기능을 안내하지 않는다.
- [ ] 지원되지 않거나 품질 인수가 안 된 유형은 제한/명시적 안내로 처리한다. 조용히 누락하지 않는다.
- [ ] 변경 전후 원본 사실 대비 카드 보존·잘못된 사실·검수 수정량·시간·비용을 동일 자료로 비교한다.

## REL-03. 버튼·페이지 체감 반응 — W 화면 주도, R 화면 별도 담당, P1

- [ ] 목록/상세/필터/저장/승인/제외/재시도의 클릭→즉시 피드백→서버 완료→화면 반영을 각각 측정한다.
- [ ] 불필요한 중복 fetch/넓은 invalidate/전체 spinner, 무관 버튼까지 막는 busy 상태를 측정 근거로 줄인다.
- [ ] 재조회 때 기존 카드·스크롤·입력을 유지한다. 성공 전에 저장/승인 완료로 표시하지 않는다.
- [ ] 승인 임베딩 대기와 단순 저장을 구분하고 해당 카드에 상태/오류/재시도를 남긴다.
- [ ] 이동/뒤로가기/새로고침/포커스 복귀/재연결/연타/느린 응답의 상태 복구와 중복 방지를 확인한다.

## REL-04. 점주 핵심 화면 정돈 — W, P1

- [ ] 업로드·작업 상태·카드 검수·목록·상세의 정보 우선순위를 정한다. 화면별 대표 행동은 하나로 만든다.
- [ ] 제목/본문/상태/근거/CTA, 여백/색/아이콘/컴포넌트를 현재 토큰으로 통일한다. 새 장식 기능은 추가하지 않는다.
- [ ] 신규/기존/충돌 카드를 비교 가능하게 하고 규격/수치/주의/원문을 쉽게 읽게 한다. 정보 축약으로 사실을 버리지 않는다.
- [ ] 빈 상태/부분 성공/실패/검수 대기/승인을 문구와 행동으로 구분한다.
- [ ] 360×800/390×844, 긴 한글/카드 30장 이상, 44px 터치/16px 본문/키보드/safe area를 실제 확인한다.

## REL-05. 파일럿 인수·배포 — W 주도, R 승인 지식 소비/질문 검토, P0 게이트

- [ ] JWT 매장 격리·승인 전 비공개·제외/과거 버전 신규 검색 차단·과거 이력 보존을 실제 API/DB로 확인한다.
- [ ] 운영 legacy reader의 안전 shim/충분성 검사/승인 RAW·안전한 비답변을 확인한다. 미패치 자유생성 경로는 사용자 파일럿에 노출하지 않는다.
- [ ] lint/typecheck/API·DB·계약/production build 및 핵심 lifecycle E2E를 실행하고 실패 시 후보를 보류한다.
- [ ] 점주 1명·직원 2명 규모의 시험 흐름과 실제 기기에서 업로드→검수→승인→학습/질문을 확인한다.
- [ ] restart/부분 실패/Storage·AI 장애의 복구, 로그/비용/오류 관측, 지원 방법을 준비한다.
- [ ] 지원 범위/제한·데이터 접근/보존·배포 대상·백업/rollback을 문서화하고 별도 운영 실행 요청을 확인한다.
- [ ] 파일럿 피드백으로 후보를 판정한다. 큰 AI 품질 개선/전체 MVP 완료로 확대 주장하지 않는다.

현재 다음 작업은 REL-G4 장기 작업 기반과 REL-00 실제 기준선이다.
사용자 범위를 넓히기 전 REL-01/02/05의 P0를 통과해야 한다.
