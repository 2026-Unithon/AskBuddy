# W/R 진행 상태와 다음 작업 — 2026-09-25

## 확인 기준

- `git pull --ff-only origin main`: Already up to date. 로컬/원격 main은 `cf4c96d44fd1d5d4e25bd6907d56c14c3f3fc941`이다.
- 9/21 PR #19 병합 이후 origin에 추가 커밋이 없다. 원격 브랜치는 main과 이미 병합된 `codex/r-sequential-handoff`뿐이며 열린 PR은 0개다. 다른 개발자의 미push 로컬 작업은 확인 범위 밖이다.
- [main CI](https://github.com/2026-Unithon/AskBuddy/actions/runs/35572226466)는 success다. 운영 배포/실자료 품질 인수를 뜻하지 않는다.
- 정본 [MVP §30~31](../ASKBUDDY_MVP_CURRENT.md), [전체 TODO](../DEV_TODO_CURRENT.md), 실제 호출부와 [9/21 전체 감사](WR_PLAN_AUDIT_20260921.md)를 대조했다. 과거 감사의 PR OPEN·R 수정 전 설명은 당시 기록이며 현재 상태는 이 문서와 [R 수정 검증](R_HISTORY_FIX_VERIFICATION_20260921.md)을 함께 본다.

## W 진행 상태

| 단계 | 구현된 범위 | 남은 범위 |
|---|---|---|
| W0 평가 기반 | 정답 감사·채점 교정·고정 분모·반복/A-A·캠페인/예산 도구 | 독립 검토·세부 커버리지, 교정된 기준선 인수. 과거 입력 미복구 해소 또는 새 캠페인 결정 |
| W1 추출·원장 | 사실 먼저 추출/저장, 구간 checkpoint, 조건/예외/부정/단위 hash, 혼합 PDF/HYBRID 입력 | 아래 재현 결함 3건, 원시 응답/잘림 복구, 위치/attempt/occurrence·재사용 키 보강 |
| W2 불변 사실 | revision/provenance schema·계약, 기존 사실/관계 제안 경로 | 실제 W 불변 fact revision 생성, entity/variant 확정, 수정/충돌/분리 이력과 OWNER_ANSWER 출처 연결 |
| W3 참조 카드 | 기존 조립·검수 및 CardPlan 타입 | 실제 CardPlan 참조 선택→서버 검사/렌더→검수 경로, occurrence 처분과 의미 편집 재승인 |
| W4 발행 | 공통 공개 서비스와 R prepare/activate 어댑터 | 실제 승인 조정자, snapshot 생산, CAS/원자 발행·색인 활성화, 제외/복원 및 legacy RAW 이관 |
| W5 품질 인수 | 실행·비교·비용 도구 | W1~W4 후보와 검토 정답으로 실제 반복/A-A, 손실/검수 부담/비용 인수 |

현재 W는 W1을 보강한 상태이며 W2~W4의 새 제품 경로 연결이 남아 있다. 기존 업로드·카드 기능이 없다는 뜻은 아니다.

이번에 실제 함수와 합성 대역으로 다시 재현한 결함:

1. **F01:** PARTIAL 자료를 작업 상세 응답으로 변환할 때 `IngestJobSourceStatus`가 거절해 ValidationError 발생 (`api/app/ingest/schemas.py:190`).
2. **F02:** 부분 실패 자료+카드가 있는 작업이 SUCCEEDED로 집계되고 재시도 SQL은 FAILED/NO_RESULT만 포함 (`job_worker.py:183`, `job_repository.py:183`).
3. **F03:** `process_source` 전처리·추출 동안 바깥 pool 연결을 계속 보유 (`pipeline.py:45`). 운영 부하 장애 자체를 재현한 것은 아니다.

W가 상태/API/UI/집계/재시도를 함께 수정하고, 외부 호출 전에 연결을 반환하는 구조로 바꿔야 한다. F01은 상세 GET, F02는 실패 구간 복구·기존 결과 중복 방지, F03은 작은 풀에서도 다른 요청/원가 저장이 진행되는 회귀로 확인한다.

## R 진행 상태

| 단계 | 구현·검증된 범위 | 남은 인수 |
|---|---|---|
| R0 | JWT/매장 격리·평가/반복/원가 도구 | 실제 승인 snapshot에 연결한 사람 정답과 기준선 |
| R1 | 승인 snapshot 소비·색인 prepare/activate·멱등성·보존 코드, 검색 결과 캐시 미사용 | 실제 W 발행/전환/철회/복구, 운영 로그·권한·30일 보존 증거 |
| R2~R4 | 슬롯/문맥·사전/hybrid·충분성·후보 rerank·행동·승인 참조 렌더·저장 전 재검사 | 자유 표현·별칭/STT·과차단·오답·인용·비용/지연 실측 및 승격 |
| R5 | 점주 원문/occurrence/outbox·claim/heartbeat/finish·FAQ/학습/알림·직원 UI | 실제 W 소비 worker와 왕복, 점주 1명/직원 2명 및 실제 Push/fallback |

- 지난 R 단독 결함(최신 대화/선택지, 앞선 점주 답변 대기 갱신, 브라우저 오류 선택자)은 수정되어 main에 반영됐다. `latest/before`, `has_pending_updates`, repeatable-read와 5초 갱신 코드/회귀가 존재한다.
- 의미 범위는 확정된 동일 대상·속성·규격·조건의 병합과 승인 후보의 대상·온도·크기 선택형 명확화까지다. 범용 의미 품질 인수 완료가 아니다.
- v2/reviewed/general semantics/reranker의 코드 기본값은 OFF다. 실제 배포 Variables는 이번에 확인하지 않았다.
- 따라서 **확인된 R 단독 결함 수정은 완료**, **R 전체 품질·운영·공동 인수는 미완료**다. 새 기능을 임의로 늘리기보다 아래 인계를 닫는 것이 다음 순서다.

## 다음 실행 순서

| 순서 | 담당 | 작업 | 선행 조건 |
|---|---|---|---|
| 1 | W | F01~F03 수정 및 실제 API/재시도/연결 수명 회귀 | 지금 가능 |
| 2 | W+R | 카드별 expected revision 매핑, 변경 카드와 전체 manifest, 공개 transaction의 activate/finish 규칙 합의 | 지금 코드/계약 검토 가능. W 초안 CAS를 immutable version ID로 대체하지 않음 |
| 3 | W | W1 잔여와 W2/W3 불변 revision·occurrence·참조 카드 생산 | 합성 자료로 개발 가능. 실제 품질 인수는 별도 |
| 4 | W 구현, R 검토 | 실제 승인→snapshot→R prepare→W 공개+R activate; 제외/복원·legacy RAW도 연결 | 2·3 선행. 실패 시 공개 전체 rollback, stale/중복 검사 |
| 5 | W 구현, R 검토 | 영속 ingest worker 및 OWNER_ANSWER 소비·지식화·검수/발행·R finish | worker 기반은 병행 가능. 공개 완료는 4 선행. 공개와 finish는 같은 transaction |
| 6 | W+R | 점주 1명·직원 2명 종단/복구·구신 전환 | 4·5 선행 |
| 병행 | 운영 담당+R | Railway 배포 SHA/Variables, 일반 로그 원문 중복 OFF, 접근 권한, DB metadata 30일 정리 증거 | 실제 환경 접근/담당 확인. 로컬 .env는 운영 증거가 아님 |
| 대기 | 회의/검토자 | RD-01 외부 AI 판정 인정 방식·사람 정답, RD-02 유료 예산/실행 담당 | 사용자의 기존 대기 결정을 유지 |
| 이후 | W+R | 실제 승인 snapshot·정답 고정 후 반복/A-A, 사전/hybrid/reranker 비교와 원가/지연 승격 판단 | snapshot+검토 정답+예산/실행 승인 |

R이 지금 맡을 일은 **2의 소비 계약·거절 반례 검토와 운영 증거 점검 준비**다. 실제 producer가 나오면 **4~6의 통합 검증**을 담당한다. W 제품 코드를 R의 독립 잔여 과제로 전가하지 않는다.

최종 경로:

```text
W 원본 → 불변 사실/참조 카드 → 점주 검수
→ R 색인 준비 → [W 원자 발행 + R 색인 활성화]
→ R 검색/답변 또는 이관 → 점주 원문 저장 + outbox
→ W worker 지식화/검수 → [W 공개 + R 완료 수신]
→ FAQ·학습·알림·재질문 갱신
```

## 이번 검증과 한계

- `audit_wr_plan_20260921.py`: F01~F03 재현. DB/provider 대역을 사용했으며 운영 DB/유료 호출 없음.
- W hash/checkpoint/부분 실패/PDF 및 R bounded semantics/external review/logging/index 계약 집중 회귀 **80 passed**. 첫 실행의 임시 폴더 권한 오류는 저장소 내부 `--basetemp`와 캐시 비활성화로 해결 후 전체 80개를 재실행했다.
- 제품 소스 검색에서 `publish_knowledge`, `activate_prepared_index`, `claim_owner_event`, `finish_owner_event`를 조합하는 W 실제 호출자는 여전히 없다. CardPlan도 계약 정의만 확인됐다.
- main CI 성공을 조회했다. 이번 로컬 점검에서 전체 DB/브라우저를 다시 실행하지 않았으며 이전 실행 수치를 새 실행으로 집계하지 않는다.
- 새 제품 수정·유료 평가·운영 변경은 수행하지 않았다. 이 문서만 새로 작성했다.
